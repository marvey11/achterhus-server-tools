{
    // Absolute path to the directory used for storing data (e.g. photos, logs, etc.).
    "storage-dir": "/path/to/storage",

    // Path to the directory used for storing service status JSON files (relative to the user's home directory).
    "status-dir": ".data/status",


    // Path to the directory where photos will be placed for processing (relative to the user's home directory).
    "photo-inbox": "photo-inbox"
}
